specVersion: "v1"
serviceId: "my-firebase-app"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "my-firebase-app-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
